import type { Meta, StoryObj } from "@storybook/react";
import Zoom from "./Zoom";

const meta: Meta<typeof Zoom> = {
  title: "ApplicationUI/Form elements/zoom",
  component: Zoom,
  tags: ["autodocs"],
};

export default meta;

type Story = StoryObj<typeof Zoom>;

export const zoom: Story = {};
  